module com.farthergate.goldberg.presets.Phlexite
import "../Config.pkl"
import "./Ruby.pkl"

function build(script: String, bundle: Boolean) = new Config.Preset {
  name = "phlexite-build"
  defaultSteps {
    (Ruby.run(bundle, script, new Listing {})) {
      name = "phlexite-build"
    }
  }
}
