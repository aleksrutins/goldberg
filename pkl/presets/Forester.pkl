module com.farthergate.goldberg.presets.Forester

import "../Config.pkl"

function build(forest: String) = new Config.Preset {
  name = "forester"
  defaultSteps {
    new {
      name = "forester"
      commands {
        new { "forester"; "build"; forest }
      }
    }
  }
}
